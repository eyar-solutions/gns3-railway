[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Railway exposes port 6080 (noVNC) as the public HTTP URL
healthcheckPath = "/"
healthcheckTimeout = 600
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
